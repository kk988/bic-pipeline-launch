# Variants pipeline

The variants pipeline should run WGS and WES pipeline. 

The pipeline code can be found here: https://github.com/kk988/adagio 

These will be the scripts necessary to run the pipeline.